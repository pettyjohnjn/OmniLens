#!/bin/bash
#PBS -N plot_intrinsic_dim
#PBS -A YOUR_PBS_ACCOUNT
#PBS -l select=1:system=YOUR_PBS_SYSTEM
#PBS -l walltime=00:20:00
#PBS -l filesystems=YOUR_PBS_FILESYSTEMS
#PBS -q debug
#PBS -j oe

set -euo pipefail

REPO_DIR="${REPO_DIR:-/path/to/project/omnilens}"

cd "${REPO_DIR}"

module use /path/to/modulefiles
module load conda
conda activate "${CONDA_ENV_NAME:-omnilens}"

export OMP_NUM_THREADS="${OMP_NUM_THREADS:-1}"
export OPENBLAS_NUM_THREADS="${OPENBLAS_NUM_THREADS:-1}"
export MKL_NUM_THREADS="${MKL_NUM_THREADS:-1}"
export NUMEXPR_NUM_THREADS="${NUMEXPR_NUM_THREADS:-1}"
export VECLIB_MAXIMUM_THREADS="${VECLIB_MAXIMUM_THREADS:-1}"
export MPLCONFIGDIR="${MPLCONFIGDIR:-/tmp/matplotlib-${USER:-omnilens}}"

ANALYSIS_DIR="${ANALYSIS_DIR:-${REPO_DIR}/analysis/intrinsic_dimensionality}"

python scripts/plot_intrinsic_dimensionality.py \
  --analysis-dir "${ANALYSIS_DIR}"
